# ================================================================
# Development profile — verbose logging, debug-friendly
# ================================================================
logging.level.com.resumainer=DEBUG
logging.level.org.springframework=INFO

# Dev email fallback: log instead of sending when RESEND_API_KEY is not set
app.email.dev-fallback-enabled=true
