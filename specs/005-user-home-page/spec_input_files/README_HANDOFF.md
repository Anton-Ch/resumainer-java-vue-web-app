# User Home OpenDesign Prototype Handoff

This folder contains the final OpenDesign live prototype for the ResumAIner User Home / Resume Workspace feature.

Use:
- `OpenCode_Implementation_Brief.md` as the main implementation instruction.
- `final-reference/resumainer-user-home.fixed.html` as the final visual reference.
- `vite-prototype/` as the source prototype reference.

Important:
- Do not copy demo routes, prototype-only footer, mock data, or CDN/static artifact structure directly into production.
- Production implementation must follow the existing Vue 3 + Vite + PrimeVue project structure.
- Real production SPA routes must live under `/app/...`.
- `/` must remain the existing landing page.